/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MystartdatestatusDirective } from './mystartdatestatus.directive';

describe('MystartdatestatusDirective', () => {
  it('should create an instance', () => {
    let directive = new MystartdatestatusDirective();
    expect(directive).toBeTruthy();
  });
});
